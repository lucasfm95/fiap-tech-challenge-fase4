apiVersion: v1
kind: Service
metadata:
  name: contact-insert-consumer-worker
spec:
  selector:
    app: contact-insert-consumer-worker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP